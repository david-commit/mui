import { Box } from "@mui/material"

const RightSideBar = () => {
  return <Box flex={2} sx={{ display: { xs: 'none', sm: 'flex', border: '1px solid red' } }}>RightSideBar</Box>;
}

export default RightSideBar